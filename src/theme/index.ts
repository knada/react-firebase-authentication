import { typeScale } from './typography'
import { primary } from './colors'

const colors = {
    primary,
}

const typography = {
    typeScale,
}

const theme = {
    colors,
    typography,
}

export default theme
